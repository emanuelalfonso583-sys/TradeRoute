# Copia este archivo como ".env" (sin ".example") y pon tu token real de Mapbox.
# El archivo ".env" NO se sube a git (está en .gitignore).
# Consíguelo gratis en: https://account.mapbox.com/access-tokens/
EXPO_PUBLIC_MAPBOX_TOKEN=pk.tu_token_aqui
